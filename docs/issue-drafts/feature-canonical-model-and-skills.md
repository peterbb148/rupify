## Summary

Define the canonical `specops-model` contract and scaffold the first orchestrator and subskills
around it.

## Acceptance Criteria

- the skill pack contains `specops`, `specops-discovery`, `specops-use-cases`, and `specops-ucp`
- the orchestrator references the canonical model rather than raw chat history
- the model contract is documented in a reference file

