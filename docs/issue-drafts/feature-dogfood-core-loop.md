## Summary

Run the first dogfooding cycle by using SpecOps to define SpecOps itself and convert that outcome
into tracked work.

## Acceptance Criteria

- `.specify/specs/001-dogfood-specops/spec.md` exists
- the GitHub issue hierarchy is aligned with the dogfooding spec
- the repo workflow explicitly treats dogfooding friction as product input

