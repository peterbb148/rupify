## Summary

Implement the deterministic UCP engine and the artifact renderer used by the SpecOps workflow.

## Acceptance Criteria

- UCP scoring is deterministic
- requirements, use-case, and estimate artifacts can be rendered from the same model
- the CLI fails clearly if YAML support is requested without the optional dependency

